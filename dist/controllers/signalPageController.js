import prisma from '../lib/prisma.js';
export const getSignalPage = async (req, res) => {
    const { slug } = req.params;
    if (typeof slug !== 'string') {
        return res.status(400).send('Invalid slug');
    }
    try {
        const signalPage = await prisma.signalPage.findUnique({
            where: { slug },
            include: { urls: true },
        });
        if (!signalPage) {
            return res.status(404).send('Signal page not found');
        }
        // Return a bot-friendly HTML page
        const linksHtml = signalPage.urls
            .map((u) => `<li><a href="${u.url}">${u.url}</a></li>`)
            .join('\n');
        const html = `
      <!DOCTYPE html>
      <html>
      <head>
        <title>Discovery Signal - ${slug}</title>
        <meta name="robots" content="index,follow">
        <style>
          body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif; line-height: 1.6; color: #333; max-width: 800px; margin: 40px auto; padding: 0 20px; }
          h1 { color: #111; border-bottom: 2px solid #eee; padding-bottom: 10px; }
          ul { list-style: none; padding: 0; }
          li { padding: 12px; border-bottom: 1px solid #eee; transition: background 0.2s; }
          li:hover { background: #f9f9f9; }
          a { color: #2563eb; text-decoration: none; font-weight: 500; word-break: break-all; }
          a:hover { text-decoration: underline; }
          .meta { font-size: 0.9rem; color: #666; margin-bottom: 20px; }
          footer { margin-top: 40px; font-size: 0.8rem; color: #999; text-align: center; }
        </style>
      </head>
      <body>
        <h1>Latest Verified Indexing Activity</h1>
        <div class="meta">Processed signal bucket: <strong>${slug}</strong></div>
        <p>The following resources have been processed and are being signaled for discovery by the Rocket-Indexer core.</p>
        <ul>
          ${linksHtml}
        </ul>
        <footer>Generated by Rocket-Indexer &bull; Enterprise Discovery Logic</footer>
      </body>
      </html>
    `;
        res.header('Content-Type', 'text/html');
        res.send(html);
    }
    catch (error) {
        res.status(500).send('Error loading signal page');
    }
};
//# sourceMappingURL=signalPageController.js.map